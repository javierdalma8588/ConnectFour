using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Piece : MonoBehaviour
{
    //private void OnCollisionEnter(Collision collision)
    //{
    //    if(collision.other.tag == "Limit")
    //    {
    //        if(GameManager._instance.AI && !GameManager._instance.player1Turns && !GameManager._instance.gameOver)
    //        {
    //            Debug.LogError("Limite");
    //            StartCoroutine(GameManager._instance.TakeTurn(0));
    //        }
    //    }

    //    if (collision.other.tag == "Piece")
    //    {
    //        if (GameManager._instance.AI && !GameManager._instance.player1Turns && !GameManager._instance.gameOver)
    //        {
    //            Debug.LogError("pieza");
    //            StartCoroutine(GameManager._instance.TakeTurn(0));
    //        }
    //    }
    //}
}
